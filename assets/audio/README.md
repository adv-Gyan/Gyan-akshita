# Wedding music

Upload the chosen background music here as:

`ambient.mp3`

The invitation is already configured to load `assets/audio/ambient.mp3`. No code change is needed after uploading the file.